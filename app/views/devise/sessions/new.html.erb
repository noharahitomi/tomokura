<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=devise-width, initial-scale=1.0" />
  <title>tomokura</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <header>
    <%= render partial:'layouts/header' %>
  </header>
  <main>
    <div class="main">
      <div class="session">
        <h2>ログイン</h2>
          <div class='login-flash-message'>
            <%= flash[:notice] %>
            <%= flash[:alert] %>
          </div>
        <%= form_with model: @user, url: user_session_path, id: 'new_user', class: 'new_user', local: true do |f| %>      
          <%= render "devise/shared/error_messages", resource: resource %>
          <div class="field">
            <label class="form-text">メール</label><br>
            <%= f.email_field :email, class:"input-email", autofocus: true, autocomplete: "email" %>
          </div>

          <div class="field">
            <label class="form-text">パスワード</label><br>
            <%= f.password_field :password, class:"input-password", autocomplete: "current-password" %>
          </div>

          <div class="actions">
            <%= f.submit "ログイン",class:"btn_log_in" %>
          </div>
        <% end %>
      </div>
    </div>
  </main>
</body>
</html>